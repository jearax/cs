---
"@jjuidev/cs": patch
---

Add Fable model support (`cs config --fable`, `ANTHROPIC_DEFAULT_FABLE_MODEL`) and write `ANTHROPIC_DEFAULT_MODEL` from the profile's Sonnet value. Fable also participates in `[1m]` context detection. Adds a README with the full CLI reference.
