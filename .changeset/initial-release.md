---
'@jearax/cs': patch
---

Initial release: cs-cli with simplified architecture (no OpenCode, no auth, cross-os env via $HOME, cs env command for env vars, cs config auto-detects [1m] model suffix).